[project]
name = "krate-extension"
version = "1.0"
description = "Python sidecar extension for Krate"
requires-python = ">=3.10"
dependencies = [
  "pikepdf==10.3.0",
]

[dependency-groups]
build = [
  "pyinstaller==6.18.0",
]

[tool.uv]
package = false
